<!-- <h1> TEST </h1> -->


<div class="container3 mt-6">
    <h1> Nom de la campagne : <strong> <%= @campaign.name %> </strong> </h1>
    <h2> Descriptif : <%= @campaign.message %> </h2>
    <h2> Hashtags associés <%= @campaign.hashtag %> </h2>
    <p> *** </p>
</div>

<div class="container">
  <div class="row">
    <div class="col-sm-8 offset-sm-2">
      <div id="campaigns">
        <% @influencers.each do |influencer| %>
          <h4><%= influencer.name %></h4>
          <p><%= influencer.community_location %></p>
        <% end %>
      </div>
    </div>
  </div>
</div>

